#pragma once
#include <M5CoreS3.h>

/**
 * @brief グローバルモード値
 * 
 * 現在表示されているアプリを識別し、各アプリ内の操作状態も管理する値
 * TopBar の右側に「MODE: 値」として表示される
 * 各アプリの setup() で設定される
 * 
 * モード設計: 100単位が主モード（アプリ種別）、1～9の位がサブモード（操作状態）
 * 主モード：
 * - 100-109: AppLock（ロック画面）
 * - 150-159: HomeScreen（アプリ一覧）
 * - 200-209: AppInfo（情報表示）
 * - 400-409: AppMotor（モーター制御）
 * - 800-899: AppManual（手動制御）
 * 
 * サブモードの例（AppMotor の場合）：
 * - 400: 初期化完了、待機状態
 * - 401: セルフチェック実行中
 * - 402: モーター選択中
 * - 410-419: タッチパネル操作中（複数モーターの詳細制御など）
 * 
 * 使用方法：
 * 1. setup() で主モードの基本値を設定
 * 2. loop() や handlePress() で操作に応じてサブモードを更新
 * 3. draw() でモード値に応じた描画を切り替え
 */
extern int mode;